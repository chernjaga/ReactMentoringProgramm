import styled, { StyledComponent } from 'styled-components';

export const FooterWrapper: StyledComponent<'footer', {}> = styled.footer`
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 16px 0;
`;