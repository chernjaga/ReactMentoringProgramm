import styled, { StyledComponent } from 'styled-components';

export const MovieDetailsPoster: StyledComponent<'img', {}> = styled.img`
    max-width: 20%;
    min-width: 300px;
    height: auto;
    display: block;
`;