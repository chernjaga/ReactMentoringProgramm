import styled, { StyledComponent } from 'styled-components';

export const MovieDetailsContent: StyledComponent<'div', {}> = styled.div`
    display: block;
    width: max-content;
    max-width: 60%;
`;